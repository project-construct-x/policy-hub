apiVersion: v2
name: policy-hub
description: Construct-X Policy Hub — Angular frontend + Spring Boot backend + PostgreSQL
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Application version — informational; the deployed image tags come from values.yaml.
appVersion: "0.0.1"
# NOTE: PostgreSQL is provided as a self-contained StatefulSet (templates/postgres-*.yaml)
# using the official postgres image, not as an external subchart. See deploy/README.md.
